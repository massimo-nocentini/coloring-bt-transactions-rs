[package]
name = "coloring-bt-transactions"
version = "0.1.0"
edition = "2021"

# std only, on purpose: this is a port of a Scheme exercise, not a dependency showcase.
[dependencies]

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
