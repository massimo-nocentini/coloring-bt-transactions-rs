rd_("EeWalk both rings from their sentinels, combining <code>p</code> and <code>q</code> \xe2\x80\xa6ClExponents.  In the bitcoin-colors driver these are block \xe2\x80\xa6DhA link: an index into <code>Arena::nodes</code>, <em>not</em> a pointer.CjA stored exponent: the exponent plus one, so that 0 is \xe2\x80\xa6BnOne color: block ids ascending, no duplicates.Eg<code>+/polynomial</code>.  The colors driver only wants <code>ior</code>, but this \xe2\x80\xa6CiSorted sets of block ids.  Same output, several times \xe2\x80\xa6AoReturns the argument unchanged.000000000BaCalls <code>U::from(self)</code>.000000000CnElements in allocations that are still referenced, and the \xe2\x80\xa6CnNodes currently reachable from some live ring \xe2\x80\x94 assuming \xe2\x80\xa6CnBuild a polynomial from terms that are already in strictly \xe2\x80\xa6BdPolynomials as circular linked listsEd<code>records</code> over <code>seconds</code>, short enough to hold a fixed column.CmA streaming reader for the transaction records the driver \xe2\x80\xa6BhWide byte scanning for the record readerDjCoefficients.  Unsigned, so <code>Arena::add</code> is only meaningful \xe2\x80\xa6CmOne input of a record: the transaction it spends, and for \xe2\x80\xa6CkCircular linked lists: the Knuth exercise, and the default.CiBlocks ascending, weights alongside.  Always the same \xe2\x80\xa6CjOne line saying whether everything the store thinks is \xe2\x80\xa6DkThe largest <code>|sum of weights - 1|</code> seen in a finished color. \xe2\x80\xa6DjEvery weight multiplied by <code>w</code>.  Ignored, and equivalent to \xe2\x80\xa6ChA second owned handle on the same color.  A copy for \xe2\x80\xa6EgRoom for <code>n</code> blocks and <code>n</code> weights, with the lengths already \xe2\x80\xa6BcWhat the driver needs a color to beDi<code>(live, committed)</code> in whatever unit the store counts, for \xe2\x80\xa6DhThe merge builds here and is frozen into an <code>Rc</code> once its \xe2\x80\xa6CnWrap the scratch buffer\xe2\x80\x99s contents as a color, and count \xe2\x80\xa6DhWhich representation of a color to run with.  See <code>store</code> \xe2\x80\xa6De<code>wa * a + wb * b</code>: union of the two supports, with the \xe2\x80\xa6CmOffered each finished color, so a store with an invariant \xe2\x80\xa6CnEvery finished color is a distribution, so its weights sum \xe2\x80\xa6BoGive a handle back.  It must not be used again.DiUnion builds here and copies into the <code>Rc</code> once the length \xe2\x80\xa6BnThe next non-whitespace byte, left unconsumed.EbWhether <code>ColorStore::combine</code> and <code>ColorStore::scale</code> do \xe2\x80\xa6CkSorted sets carrying a weight per block.  Different output.ClNodes the arena has committed to, including those on the \xe2\x80\xa6BbColors as sorted sets of block idsDcBytes consumed strictly before <code>buf[pos]</code>, for error \xe2\x80\xa6DiDecimal, straight into the line buffer.  <code>write!</code> would do \xe2\x80\xa6ClRead one non-negative integer.  Nothing in these records \xe2\x80\xa6ChNodes in the ring, sentinel included.  Diverges on a \xe2\x80\xa6CcConsume one integer without working out what it is.BoColors as weighted distributions over block idsDkThe circular-list backend: <code>crate::poly::Arena</code> plus the one \xe2\x80\xa6DcHow many bytes at the front of <code>bytes</code> are ASCII digits.CfWalk the run of digits at the cursor, handing each \xe2\x80\xa6DjHand a ring back for reuse.  The caller must not hold <code>root</code>\xe2\x80\xa6CkThe color of a coinbase transaction: the one block that \xe2\x80\xa6CjA place colors live, and everything the driver does to \xe2\x80\xa6CnA fresh sentinel pointing at itself: the empty ring, ready \xe2\x80\xa6DjMerge two ascending duplicate-free slices into <code>out</code>, which \xe2\x80\xa6Dg<code>dst[k] = src[k] * factor</code>, for as many elements as both \xe2\x80\xa6CcHow often <code>--stats</code> reports, in records.EbThe next record, or <code>None</code> at end of input.  <code>inputs</code> is \xe2\x80\xa6DnA weight in <code>[0, 1]</code>, to <code>WEIGHT_PLACES</code> fixed decimals.CkPositions an input is read for: the amount at 1 and the \xe2\x80\xa6CmBelow every legal key, which is what makes the merge free \xe2\x80\xa6Dg<code>wa * a + wb * b</code> into the staging slices, answering how \xe2\x80\xa6DgThe value of eight ASCII digits packed into <code>chunk</code>, the \xe2\x80\xa6DkThe Scheme stops at <code>(&gt; i 1000000)</code>, i.e. after 1,000,001 \xe2\x80\xa6ClPositions the record header is read for: the block id at \xe2\x80\xa6CkAn output is read for nothing at all \xe2\x80\x94 only for there \xe2\x80\xa6BoHow many decimal places a weight is printed to.BkVisit the blocks in decreasing order as \xe2\x80\xa6CnVisit the terms in decreasing exponent order, skipping the \xe2\x80\xa6Ao<code>digit_run</code> on NEON.DjRead <code>( int int ... )</code>, handing the elements whose position \xe2\x80\xa6Dh<code>dst[k] = a[k] * fa + b[k] * fb</code>, for as many elements as \xe2\x80\xa6ClThe obviously-correct version, and the oracle the vector \xe2\x80\xa6AjBitcoin transaction colors")