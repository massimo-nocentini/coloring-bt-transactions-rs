rd_("EeWalk both rings from their sentinels, combining <code>p</code> and <code>q</code> \xe2\x80\xa6ClExponents.  In the bitcoin-colors driver these are block \xe2\x80\xa6DhA link: an index into <code>Arena::nodes</code>, <em>not</em> a pointer.CjA stored exponent: the exponent plus one, so that 0 is \xe2\x80\xa6BnOne color: block ids ascending, no duplicates.Eg<code>+/polynomial</code>.  The colors driver only wants <code>ior</code>, but this \xe2\x80\xa6AoReturns the argument unchanged.00000BaCalls <code>U::from(self)</code>.00000CnElements in allocations that are still referenced, and the \xe2\x80\xa6CnNodes currently reachable from some live ring \xe2\x80\x94 assuming \xe2\x80\xa6CnBuild a polynomial from terms that are already in strictly \xe2\x80\xa6BdPolynomials as circular linked listsEd<code>records</code> over <code>seconds</code>, short enough to hold a fixed column.CmA streaming reader for the transaction records the driver \xe2\x80\xa6BhWide byte scanning for the record readerDjCoefficients.  Unsigned, so <code>Arena::add</code> is only meaningful \xe2\x80\xa6CjOne line saying whether everything the store thinks is \xe2\x80\xa6ChA second owned handle on the same color.  A copy for \xe2\x80\xa6BcWhat the driver needs a color to beCeSet union.  Borrows both operands and leaves them \xe2\x80\xa6Di<code>(live, committed)</code> in whatever unit the store counts, for \xe2\x80\xa6CnWrap the scratch buffer\xe2\x80\x99s contents as a color, and count \xe2\x80\xa6BoGive a handle back.  It must not be used again.DiUnion builds here and copies into the <code>Rc</code> once the length \xe2\x80\xa6BnThe next non-whitespace byte, left unconsumed.ClNodes the arena has committed to, including those on the \xe2\x80\xa6BbColors as sorted sets of block idsDcBytes consumed strictly before <code>buf[pos]</code>, for error \xe2\x80\xa6DiDecimal, straight into the line buffer.  <code>write!</code> would do \xe2\x80\xa6ClRead one non-negative integer.  Nothing in these records \xe2\x80\xa6ChNodes in the ring, sentinel included.  Diverges on a \xe2\x80\xa6CcConsume one integer without working out what it is.DkThe circular-list backend: <code>crate::poly::Arena</code> plus the one \xe2\x80\xa6DcHow many bytes at the front of <code>bytes</code> are ASCII digits.CfWalk the run of digits at the cursor, handing each \xe2\x80\xa6DjHand a ring back for reuse.  The caller must not hold <code>root</code>\xe2\x80\xa6CkThe color of a coinbase transaction: the one block that \xe2\x80\xa6CjA place colors live, and everything the driver does to \xe2\x80\xa6CnA fresh sentinel pointing at itself: the empty ring, ready \xe2\x80\xa6DjMerge two ascending duplicate-free slices into <code>out</code>, which \xe2\x80\xa6CcHow often <code>--stats</code> reports, in records.EbThe next record, or <code>None</code> at end of input.  <code>inputs</code> is \xe2\x80\xa6CgPosition an input is read for: the previous tx id at 2.CmBelow every legal key, which is what makes the merge free \xe2\x80\xa6DgThe value of eight ASCII digits packed into <code>chunk</code>, the \xe2\x80\xa6DkThe Scheme stops at <code>(&gt; i 1000000)</code>, i.e. after 1,000,001 \xe2\x80\xa6ClPositions the record header is read for: the block id at \xe2\x80\xa6CkAn output is read for nothing at all \xe2\x80\x94 only for there \xe2\x80\xa6BkVisit the blocks in decreasing order as \xe2\x80\xa6CnVisit the terms in decreasing exponent order, skipping the \xe2\x80\xa6Ao<code>digit_run</code> on NEON.DjRead <code>( int int ... )</code>, handing the elements whose position \xe2\x80\xa6ClThe obviously-correct version, and the oracle the vector \xe2\x80\xa6AjBitcoin transaction colors")